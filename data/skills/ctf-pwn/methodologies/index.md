# CTF Pwn Methodology Index

Add one row per completed challenge. Use this file before starting a new solve so you can search by mitigation mix, primitive, and reusable tags.

## Canonical Tags

Use only these tags unless a new tag is clearly needed:

- `stack-overflow`
- `fmt`
- `uaf`
- `double-free`
- `heap-overflow`
- `off-by-one`
- `ret2libc`
- `rop`
- `canary-bypass`
- `pie-leak`
- `seccomp`
- `srop`
- `fsop`
- `tcache`
- `signed-index`
- `orw`
- `auth-bypass`
- `cmd-injection`

## Index

| challenge | arch | mitigations | vuln_class | primitives | decisive_insight | reuse_tags | file |
| --- | --- | --- | --- | --- | --- | --- | --- |
| encryption | x86_64 | NX, Partial RELRO, No PIE, No Canary, SHSTK, IBT | input-first chosen-plaintext oracle with same-second key reuse and an irrelevant stack overflow | same-second chosen-plaintext oracle, affine GF(2) matrix recovery, direct blockwise decryption | When a one-shot block cipher service reuses the same seeded transform for every reconnect within one second, a fixed nonzero baseline plus same-second bit-flip queries is enough to recover the full affine matrix and decrypt the flag directly. | stack-overflow | methodologies/encryption_2026-04-04.md |
| attachment_2 | x86_64 | NX, Partial RELRO, No PIE, No Canary, SHSTK, IBT | format string in global `.bss` buffer | `_exit@got` partial overwrite to recursive `main`, libc leak from recursive frame, single-call `printf@got -> system` dual overwrite | The first fmt write is only bootstrap; once `_exit@got` re-enters `main`, the recursive frame's `arg10 -> slot18` and `arg26 -> slot44` aliases are the shortest way to finish `printf@got -> system` in one call. | fmt | methodologies/attachment_2_2026-04-04.md |
| pwn_ct | mixed | aarch64 nginx PIE, Full RELRO, Canary, NX, FORTIFY; x86_64 static Go backend | nginx alias traversal plus sed replacement-expression injection | config leak via alias traversal, HS256 JWT forgery, sed `#e` command execution, arbitrary file read via command output | Once `/static../app/config.yaml` leaked the JWT secret, the shortest route was to treat `/api/config/update` as a sed-script injection sink: `new_value` could inject `#e` into `sed 's#.*old.*#new#'` and turn the helper into authenticated command execution. | cmd-injection | methodologies/pwn_ct_2026-03-28.md |
| attachment_3 | x86_64 | NX, Partial RELRO, No PIE, No Canary, SHSTK, IBT, seccomp | custom softlink metadata confusion | arbitrary read/write via forged softlink blob, libc leak, `environ` stack leak, saved-RIP overwrite, raw-syscall ORW | Once `SOFTLINK` was shown to deserialize attacker-controlled file contents into `ptr/size/perm`, the shortest route was libc leak -> `environ` leak -> overwrite `menu_loop`'s saved RIP with a raw-syscall ORW chain, because glibc `open()` would hit forbidden `openat`. | rop, seccomp, orw | methodologies/attachment_3_2026-04-04.md |
| pwn_throne_hazard | x86_64 | NX, Full RELRO, No PIE, Canary, FORTIFY, SHSTK, IBT, seccomp | TOCTOU-gated heap overflow into a heap-resident dispatch object | race-triggered heap overflow, copy-relocated stdout leak, dispatch-table retargeting, ORW via open/readv/write | Once seccomp killed the tempting `system`/`execve` route, the shortest path was to keep lane 0 as the built-in `write`, repurpose lanes 2 and 3 to `open` and `readv`, and use the raced actuator as a tiny ORW syscall multiplexer. | heap-overflow, orw | methodologies/pwn_throne_hazard_2026-03-28.md |
| pwn_ph | x86_64 | NX, Partial RELRO, PIE (`vuln.so`), Apache `mod_php` 8.1 | signed index OOB into extension metadata | cross-request base leak, negative-index arbitrary `.data/.bss` write, GOT overwrite to `system`, file-based flag exfil | The decisive route was `edit(-40)`: `heap[-40]` dereferenced a self-pointer relocation at `base+0x4080`, so one negative-index edit rebuilt `.data/.bss`, seeded `heap[0] = _efree@got`, and turned the next `delete()` into `system("/readflag > /var/www/html/...")`. | signed-index, ret2libc | methodologies/pwn_ph_2026-03-28.md |
| mini_mqtt | x86_64 | NX, Full RELRO, PIE, Canary, SHSTK, IBT | signed-length bypass plus stale global command-tail reuse | negative `ContentLength` length bypass, staged global command injection, remote shell-command execution via `popen` | If a signed `%d` length is sign-extended and then checked with `jbe`, a negative value can stage an overlong shell command; when the binary later `memcpy`s a shorter allowlisted command into the same global buffer without writing a NUL, the stale tail survives and turns the next `popen()` into command injection. | cmd-injection | methodologies/mini_mqtt_2026-03-28.md |
| httpd | x86_64 | NX, Partial RELRO, No PIE, Canary, SHSTK, IBT | auth bypass plus stack overflow plus caller-frame reuse | empty-token auth bypass, bytewise canary and saved-`rbp` brute, caller-local overwrite leak, final ret2libc `system(token)` | In a fork-per-connection daemon, treat saved `rbp` like another canary byte stream: stop the overwrite after each tested byte, then reuse the repaired caller frame at `402ac5` to leak both libc and the shared token pointer. | stack-overflow, canary-bypass, ret2libc, rop, auth-bypass | methodologies/httpd_2026-03-28.md |
| ezheap | x86_64 | NX, PIE, Full RELRO, Canary, FORTIFY, SHSTK, IBT, seccomp | heap integer truncation plus heap overflow plus UAF | tiny-chunk overflow into `scheduler.ctrl`, tcache poisoning onto live task descriptor, built-in flag-reader call | Disable the scheduler whitelist with the artifact overflow first, then use the freed-session `0x60` tcache poison to reallocate a worker profile on top of `task0`, because worker profiles and task descriptors share the same writable layout. | heap-overflow, uaf, tcache, seccomp | methodologies/ezheap_2026-03-28.md |
| interpreter | x86_64 | NX, No RELRO, No PIE, No Canary, static, RWX segment | vector/string OOB against host-stack-allocated VM objects | raw stack-pointer leak via `vector-set!`, host-stack OOB read/write, lambda-offset corruption, host-stack ret2win | `vector-set!` leaks an untagged stack pointer, and lambda arity truncation gives a free `>> 4`, so you can patch a closure offset and make `call` pivot `rsp` onto a fake host stack. | rop | methodologies/interpreter_2026-03-28.md |
| whatsthetime | i386 | NX, Partial RELRO, No PIE, No Canary | stack overflow behind time-seeded XOR | banner-derived XOR seed, encoded return-address overwrite, direct ret2plt(system) | The printed `ctime()` banner leaks the minute-rounded XOR seed, and the fake win path only runs `ls`, so the shortest route is an encoded `system@plt("/bin/sh")` chain. | stack-overflow, rop | methodologies/whatsthetime_2026-03-28.md |
| ez_nc | x86_64 | NX, Full RELRO, PIE, No Canary, SHSTK, IBT | format string in filename synthesis | stack leak, format-string-driven filename synthesis, remote binary recovery | If attacker input is the `snprintf` format string for a later `fopen`, probe deep `%s` positions first: `%45$s` synthesized the executable path and turned a remote-only service into a downloadable ELF. | fmt | methodologies/ez_nc_2026-03-28.md |
| ez_nc | x86_64 | NX, Full RELRO, PIE, No Canary, SHSTK, IBT | format string in filename synthesis | format-string-driven filename synthesis, remote binary recovery, static flag extraction | The current service still falls to `%45$s`: once the filename formatter resolves a startup-stack path, the fastest route is to download the PIE and harvest the embedded flag from `.rodata`. | fmt | methodologies/ez_nc_2026-03-30.md |
| build | x86_64 | NX, Partial RELRO, No PIE, No Canary | stack overflow | stack leak, arbitrary stack write target, self-amplified read, ret2libc | Point the tiny final read at its own return address, pump `nbytes` once, then reuse the persisted global length for a one-shot second-stage ret2libc. | stack-overflow, ret2libc, rop | methodologies/build_2026-03-21.md |
| attachment | x86_64 | NX, Full RELRO, No PIE, Canary, SHSTK, IBT | stack overflow plus echo-based overread | canary leak, saved-rbp leak, libc leak via stdout copy relocation, staged ret2libc ROP | Leak libc through the executable's copied stdout pointer, then stop using stdio because the same fake-frame read that enabled the leak also corrupts those globals. | stack-overflow, canary-bypass, ret2libc, rop | methodologies/attachment_2026-03-25.md |
| odd_chat | x86_64 | NX, Partial RELRO, No PIE, Canary | signed-length bypass to heap overflow | arbitrary-length heap write via `INT_MIN`, libc leak via forged list node, tcache `fd` poisoning, GOT overwrite to `system` | Model the branchless `abs` on `INT_MIN`: it leaves the chat length at `-8`, and the later unsigned loop turns that one edge case into a newline-terminated arbitrary heap write. | heap-overflow, tcache | methodologies/odd_chat_2026-03-26.md |
| neural | x86_64 | NX, Full RELRO, PIE, Canary, SHSTK, IBT | deterministic admin-key derivation plus VNM command-buffer overwrite | public `pid` and uptime leak, admin token forgery, signed-offset dword write into `g_pwn+0x10`, root `system()` execution | Recompute the per-boot admin secret from the public status API, then use the diagnostics VM's signed-offset store to rewrite the command string that root later passes to `system()`. | auth-bypass | methodologies/neural_2026-03-26.md |
| speedpwn | x86_64 | NX, Partial RELRO, No PIE, Canary, SHSTK, IBT | forward OOB bit write plus uninitialized-value oracle | comparator-based libc leak, forward bit-level arbitrary write, fake `_IO_FILE` stdin bulk write, GOT overwrite to `system` | Reconstruct the hidden libc return address through `simulate()` and the custom `cmp()` oracle, then use a fake `_IO_file_jumps` object so the next `fread` rewrites `srand@got` and turns `reseed()` into `system(game_history)`. | fsop, pie-leak | methodologies/speedpwn_2026-03-21.md |
| src_bts | x86_64 | CPython 3.11 service under ASLR; no seccomp observed | ctypes stale pointer / uaf | heap pointer leak, large relative byte write, Python code-object tuple overwrite | Rewrite `main.__code__.co_names` so later `int(...)` calls resolve the existing builtin name `eval`, turning a stale-pointer relative write into immediate code execution. | uaf | methodologies/src_bts_2026-03-23.md |
| tiny | x86_64 | NX, Full RELRO, PIE, No Canary | stack overflow plus path traversal | arbitrary file read, maps leak, exact stack-buffer leak, ret2libc | Leak the exact `file` stack-buffer address from one forked child through `/proc/self/mem`, then reuse that stable address in the next child for a short `system(file_buf)` chain. | stack-overflow, pie-leak, ret2libc, rop | methodologies/tiny_2026-03-21.md |
| ret2sender | x86_64 | Partial RELRO, No PIE, No Canary, Executable Stack, SHSTK, IBT | stack overflow | saved return-address overwrite, controlled function argument via ROP | Ignore the executable-stack bait: a 40-byte overflow plus `ret -> pop rdi -> drive(0x48435344)` is the shortest route because the binary already contains both the win function and the needed gadget. | stack-overflow, rop | methodologies/ret2sender_2026-03-28.md |
| pwn_treasure | x86_64 | NX, Partial RELRO, PIE, Canary, SHSTK, IBT | signed index arbitrary write with same-address `%s` leak | signed OOB qword write, libc leak via copy-relocated `stdout`, constrained libc `one_gadget` | Leak `_IO_2_1_stdout_` from the copied stdio slot first, then use the later name buffer to satisfy a libc `one_gadget` and spend the final write on `printf@got`. | ret2libc | methodologies/pwn_treasure_2026-03-28.md |
| music_box | x86_64 | NX, PIE, Partial RELRO, Canary, SHSTK, IBT | TOCTOU stale-offset race against a compacting custom allocator | stale offset read, stale offset write, note-header forgery, slot-metadata arbitrary read/write, PIE leak, libc leak, GOT overwrite to `system` | Treat the no-lock slot offset as a stale capability: race it across GC compaction so the checksum-fallback path lands eight bytes onto another note header, then pivot that forged note into the slot-metadata table. | heap-overflow, pie-leak, ret2libc | methodologies/music_box_2026-03-29.md |
| ezheap | x86_64 | NX, PIE, Full RELRO, Canary, SHSTK, IBT | heap off-by-one null with largebin metadata abuse | libc leak, heap leak, pseudo-tcache arbitrary aligned malloc, stack leak via `program_invocation_short_name`, active-`read` return overwrite, ret2libc | Once the largebin attack turns `mp_.tcache_bins` into a pseudo tcache for the oversized class, the shortest stable finish is to leak `argv[0]` from `program_invocation_short_name` and overwrite the live single-threaded libc `read()` return address, not `edit_chunk`'s saved RIP. | off-by-one, tcache, rop, ret2libc | methodologies/ezheap_2026-04-04.md |
| orangeforce | x86_64 | NX, PIE, Full RELRO, Canary | heap overflow plus OOB heap read | heap leak, libc leak, House of Force, arbitrary top-chunk relocation, libc hook overwrite | On socket-backed `scanf` plus `read` services, make the large House-of-Force payload whitespace-safe first; once residual bytes stop desynchronizing the menu, the usual glibc-2.27 top-chunk math works again. | heap-overflow, ret2libc | methodologies/orangeforce_2026-04-07.md |
| attachment_30 | x86_64 | NX, PIE, No RELRO, Canary, SHSTK, IBT | signed index OOB against a global note table | libc leak from copy-relocated `stdin`, GOT overwrite via negative index, `system("/bin/sh")` | If a negative note index reaches copy-relocated stdio pointers, leak libc there first, then validate the live remote libc revision with a GOT leak before spending the arbitrary write on `puts@got -> system`. | signed-index, ret2libc | methodologies/attachment_30_2026-05-09.md |
| borrowstack | i386 | NX, Partial RELRO, No PIE, No Canary | stack overflow | contiguous GOT leak via `puts(read@got)`, libc fingerprint, ret2libc `system("/bin/sh")` | If `puts(read@got)` spills across adjacent `.got.plt` slots, one first-stage leak can fingerprint the whole remote libc and make plain ret2libc shorter than `ret2dlresolve`. | stack-overflow, ret2libc, rop | methodologies/borrowstack_2026-05-13.md |
| ring_factory | x86_64 | NX, PIE, Full RELRO, Canary | short format-string canary leak plus UAF and uninitialized heap leak plus stack overflow | canary leak via `%7$p`, heap leak from tcache metadata, libc leak from unsorted-bin metadata, final ret2libc `system("/bin/sh")` | When the only format-string budget is five bytes, spend it on `%7$p` for the canary and let stale fixed-size heap slots print allocator metadata; a guard allocation behind the eighth free turns that UAF into the libc leak needed for the final stack-smash ret2libc. | fmt, uaf, tcache, ret2libc, rop | methodologies/ring_factory_2026-05-13.md |
| deepvoid | x86_64 | NX, Partial RELRO, No PIE, Canary, FORTIFY | heap overflow into old glibc unlink | unsafe unlink table pivot, GOT leak via `free@got -> puts@plt`, parser hijack via `strtol@got -> system`, shell spawn via raw `sh\\x00` | If an old-glibc unsafe unlink can pivot a global slot table onto GOT entries, rewriting `strtol@got` to `system` is a shorter finish than repointing a slot to `"/bin/sh"`, but only when the parser trigger is sent as a NUL-terminated raw string. | heap-overflow, ret2libc | methodologies/deepvoid_2026-05-14.md |
